[pytest]
testpaths = src/tests
python_files = test_*.py
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    scenario: end-to-end investigation scenarios replayed from fixtures
