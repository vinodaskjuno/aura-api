fastapi>=0.110
uvicorn>=0.29
# boto3 is what makes Aura start the AWS Floci emulator: `clouds_for()` reads the
# Dependency nodes code analysis writes from this file, so the emulator selection is
# derived from the code rather than configured anywhere.
boto3>=1.34

# Multi-cloud. Uncommenting these makes Aura start three emulators instead of one,
# which demonstrates that selection follows the dependencies — at the cost of two more
# containers per run. The /assets and /blobs routes degrade on their own when the
# matching emulator is absent, so leaving these commented costs two cases, not the run.
# google-cloud-storage>=2.16
# azure-storage-blob>=12.19
