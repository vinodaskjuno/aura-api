; ─────────────────────────────────────────────────────────────────────────
;  OntoVerse — Bedrock credentials (single paste point)
;  Copy this file to  credentials.ini  and paste your AWS block below.
;  Paste EXACTLY the format you get (profile header + the three keys).
;  The engine reads the FIRST [section] it finds. credentials.ini is git-ignored.
;  Rotate temporary STS keys after use.
; ─────────────────────────────────────────────────────────────────────────

[136052625764_BUDeveloper]
aws_access_key_id=PASTE_HERE
aws_secret_access_key=PASTE_HERE
aws_session_token=PASTE_HERE
; optional overrides:
; region=us-east-1
; Must be an inference-profile id (us. / eu. / apac. prefix), not a bare
; foundation-model id — Bedrock rejects bare ids for current Anthropic models.
; bedrock_model_id=us.anthropic.claude-sonnet-4-5-20250929-v1:0
